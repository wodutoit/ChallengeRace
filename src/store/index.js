import Vue from 'vue';
import Vuex from 'vuex';
import dataStore from './dataStore';

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
    dataStore,
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  },
});
